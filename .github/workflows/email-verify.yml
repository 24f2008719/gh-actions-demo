name: Email Verification Action

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f2008719@ds.study.iitm.ac.in
        run: echo "Hello, world!"
